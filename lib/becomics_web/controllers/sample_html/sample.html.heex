<%= for c <- @comics do %>
  <br />
  <.form :let={f} for={c} action={~p"/api/postcomic/" <> Map.get(c, "id")}>
    <a href={Map.get(c, "url_for_href")} target="_ignore"><%= Map.get(c, "name") %></a>
    <.input field={f[:url]} />
    <input type="submit" />
  </.form>
<% end %>
